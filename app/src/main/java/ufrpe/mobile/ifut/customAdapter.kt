package ufrpe.mobile.ifut

import android.support.v7.widget.RecyclerView
import android.view.View
import android.widget.ImageView

/*class CustomAdapter(val amigosList: ArrayList<Amigo>):RecyclerView.Adapter<CustomAdapter.ViewHolder>() {
    override fun onBindViewHolder(p0?: ViewHolder?,p1:Int){
        //val amigo: Amigo = amigoList[p1]

        //p0?.textViewName.text=amigo.nome
        //p0?.foto.setImageResource(amigo.foto)
    }
    override fun onCreateViewHolder(p0:ViewGroup?,p1:Int): ViewHolder{
        //val v = LayoutInflater.from(p0?.context).inflate(R.layout.list_layout,p0,false)
        //return viewHolder(v)
    }
    override fun getItemCount(): Int{
        return userList.size
    }
    class ViewHolder(itemView: View): RecyclerView.ViewHolder(itemView){
       // val textViewName = itemView.findViewById(R.id.textViewName) as TextView
       // val foto = itemView.findById(R.id.fotoAmigo) as ImageView
    }

}*/